<% @products.each do |product| %>
	<div class="post_wrapper">
		<h2 class="Title"><%= link_to product.item, product %></h2>
		<p class="date"><%= product.created_at.strftime("%B, %d, %Y") %></p>
	</div>
<% end %>